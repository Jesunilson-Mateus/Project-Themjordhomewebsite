# Estes ficheiros são só para o painel de administração ler/escrever.
# Ninguém deve conseguir abri-los diretamente no browser.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
